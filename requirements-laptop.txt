# ============================================================
# Windows 11 Laptop: MT5-Verbindung & Live-Trading
# Installieren mit: pip install -r requirements-laptop.txt
# WICHTIG: Nur auf Windows ausführen! MetaTrader5 läuft nicht auf Linux.
# ============================================================

# MT5-Verbindung (nur Windows!)
MetaTrader5>=5.0.45

# Datenverarbeitung
pandas>=2.0.0
numpy>=1.24.0

# Technische Indikatoren (für Live-Feature-Berechnung)
# WICHTIG: pandas_ta SEPARAT installieren mit --no-deps (siehe Install-Anleitung unten)!
# Grund: pandas_ta 0.4.x hängt von numba ab → numba unterstützt Python 3.14 NICHT.
# numba ist nur eine optionale Beschleunigung; RSI, ATR, BB etc. laufen ohne numba.
# Installieren mit: pip install pandas_ta --no-deps
# pandas_ta ist NICHT in dieser Datei gelistet – sonst zieht pip numba mit rein!

# ML-Modelle ausführen (muss gleiche Version wie Server haben!)
lightgbm>=4.6.0  # Server-Version: 4.6.0 – gleiche Major.Minor verwenden!
scikit-learn>=1.3.0  # Pflicht! joblib braucht sklearn zum Deserialisieren der Modelle
joblib>=1.3.0

# HTTP-Anfragen (für Fear & Greed + BTC Funding Rate APIs)
requests>=2.28.0

# Umgebungsvariablen / API-Keys
python-dotenv>=1.0.0
